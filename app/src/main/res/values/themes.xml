<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.VitruvianRedux" parent="android:Theme.Material.Light.NoActionBar">
        <!-- Show a simple splash background while the app draws its first frame (pre-Android 12) -->
        <item name="android:windowBackground">@drawable/splash_background</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowFullscreen">false</item>
    </style>
</resources>